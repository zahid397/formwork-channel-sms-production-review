package one.formwork.base.tenant.config;

import org.springframework.boot.autoconfigure.AutoConfiguration;

/**
 * STUB - reconstructed for this review only. formwork-channel-sms only needs
 * this type to exist as an ordering anchor
 * ({@code @AutoConfiguration(after = TenantBaseAutoConfiguration.class)}); the
 * real platform class presumably also sets up tenant context resolution.
 */
@AutoConfiguration
public class TenantBaseAutoConfiguration {
}
